package test;

import dbmodel.BookDB;
import dbmodel.CustomerDB;
import dbmodel.ReviewDB;
import model.Book;
import model.Customer;
import model.Review;

public class TestReview {
    public static void main(String[] args) {
//        Customer c1 = CustomerDB.getInstance().selectByID(1);
//        Book b1 = BookDB.getInstance().selectByID(1);
//        Review r = new Review(4, "Dịch vụ tốt", c1, b1);
//        // lưu xuống csdl
//        ReviewDB.getInstance().insert(r);
//        
//        // lấy review 
//        Review newr = ReviewDB.getInstance().selectByID(1);
//        System.out.println(newr.getRate() + " " + newr.getDescription() + " " 
//                + newr.getCustomer().getFullName() + " " + newr.getBook().getTitle());
    }
}
